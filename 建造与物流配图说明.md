# 建造与物流白色线稿

- 当前文件：`assets/construction-logistics-white-4k.png`。
- 尺寸：4096 × 4096，RGBA；所有可见线条 RGB 均为255、255、255，alpha 范围0–255。
- 空白区域：约76.59%像素完全透明，四周至少140像素为完全透明留白，非零alpha边界为(161, 200, 3955, 3893)。
- 内容：方形构图，起重机、建筑框架、机械臂、运输车和箱子；白色粗线条，不含未来科幻装饰。
- 生成方式：内置 `image_gen` 新图生成，再进行线条简化修正；所选源图为工作区的 `output/imagegen/construction-logistics-white-v2-clean.png`。生成工具的后续透明背景处理未成功，其结果未用于网页。
- 本地处理：用户明确授权后，使用 Pillow 与 NumPy 清除源图中的灰色棋盘底纹。提取亮白线条，以alpha渐变保留平滑边缘，并将线条颜色统一为纯白。未改动原始源图。
- 可重复执行的处理脚本：初次去底纹使用工作区 `tmp/poster-research/clean_construction_lineart.py`；4K高清处理使用 `tmp/poster-research/enhance_construction_lineart.py`。
- 高清处理：基于1254 × 1254透明线稿清理细小边缘杂点，以轮廓平滑和8192像素中间采样生成4096 × 4096抗锯齿PNG。属于对原线稿的高清重采样，非重新生成场景；回到原图尺寸核对，轮廓重合度约99.83%，面积变化约0.11%。原版PNG保留。
- 网页显示：原最大宽度420像素，现最大宽度520像素，并按首屏可用高度等比完整显示。
- 核验：检查真实alpha透明度、边缘留白、白色线条与深蓝底叠加效果、文件复制完整性及网页资源引用。本次未执行浏览器排版测试。

## 所选线稿的实际修正提示词

```text
Use case: precise-object-edit.
Edit the attached construction illustration to repair its malformed linework and transparency. Preserve the same compact square arrangement and familiar practical objects: construction crane above, rectangular building frame, industrial robot arm holding a structural block, and a low wheeled transport cart with two boxes at the shared base.
Replace the existing distressed, mottled, double-edged drawing with elegant SIMPLE BOLD WHITE SINGLE-STROKE OUTLINES. Each structural beam, robot segment, block and box must be hollow inside: clean transparent interior, one deliberate even white outline around its silhouette, and only the few essential white internal lines needed for recognition. Approximately 10–12 px even stroke weight at 1024 px canvas. Rounded line caps and joins. Remove redundant internal edges, technical truss hatching, decorative marks and small detail. Make major forms easy to read.
Completely erase ALL irregular white fill, all white blotches, all tiny speckles, all duplicate hairline edges, all grain and all distress texture. No shading, no gray, no shadows, no glow, no 3D rendering, no black lines. All visible strokes pure white RGB 255,255,255; use alpha only for edge antialiasing.
Deliver actual transparent RGBA PNG: completely empty background and ALL shape interiors have alpha 0. Do not simulate transparency with black, white, gray, checkerboard, or any other opaque backdrop. This white illustration will be displayed on a dark blue webpage, but do not include that blue background in the file.
Keep the object group square and balanced, repair clipped edges, and fit every illustrated mark within a clean approximately 5% transparent margin on all four sides. No marks may touch the canvas edges. Ordinary contemporary construction and warehouse equipment, not futuristic or sci-fi. No text, logos, or watermark. This is one repair of the existing composition, not a new concept.
```

提示词中的透明背景要求未由生成工具完成；当前交付文件的真实透明背景由上述用户授权的本地处理实现。
